# CLI v2 Managed Online Endpoint — the stable REST URL + auth (no model attached yet).
# Endpoint names must be unique within an Azure region for your subscription.
# Docs: https://learn.microsoft.com/azure/machine-learning/reference-yaml-endpoint-online?view=azureml-api-2
$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineEndpoint.schema.json
name: iris-endpoint-mlops
description: Iris species prediction endpoint (managed online, deployed via Azure DevOps)
auth_mode: key
